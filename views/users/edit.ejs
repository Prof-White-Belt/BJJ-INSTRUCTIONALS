<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile - BJJ Instructionals</title>

    <!-- Link to Global CSS -->
    <link rel="stylesheet" href="/stylesheet/global.css" />
    <!-- Link to Profile Edit Page CSS -->
    <link rel="stylesheet" href="/stylesheet/edit.css" />
    <link rel="stylesheet" href="/stylesheet/navbar.css" />

  </head>

  <body>
    <!-- Navbar -->
    <%- include('../partials/navbar') %> <!-- Corrected path for your setup -->

    <div class="edit-profile-page">
      <div class="edit-profile-card">
        <h1>Edit Profile</h1>

        <form method="POST" action="/users/<%= user._id %>?_method=PUT" class="edit-profile-form">
          <label for="age">Age:</label>
          <input type="number" name="age" value="<%= user.age %>" /><br />

          <label for="rank">Rank:</label>
          <select name="rank" required>
            <option value="White" <%= user.rank === "White" ? "selected" : "" %>>White</option>
            <option value="Blue" <%= user.rank === "Blue" ? "selected" : "" %>>Blue</option>
            <option value="Purple" <%= user.rank === "Purple" ? "selected" : "" %>>Purple</option>
            <option value="Brown" <%= user.rank === "Brown" ? "selected" : "" %>>Brown</option>
            <option value="Black" <%= user.rank === "Black" ? "selected" : "" %>>Black</option>
          </select><br />

          <label for="bio">Bio:</label>
          <textarea name="bio"><%= user.bio %></textarea><br />

          <button type="submit">Update</button>
        </form>
      </div>
    </div>

    <!-- Footer -->
    <%- include('../partials/footer') %> <!-- Footer included here -->
  </body>
</html>
